{
  "name": "eshop",
  "version": "1.0.0",
  "description": "A reference .NET application implementing an eCommerce web site using a services-based architecture.",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "loadgen": "playwright test --config=playwright.loadgen.config.ts --project=loadgen",
    "loadgen:auth": "playwright test --config=playwright.loadgen.config.ts --project=setup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^20.11.25",
    "dotenv": "^16.4.5"
  }
}
